// The Infrarust plugin world: the host/guest contract.
// Versioned and frozen; deferred surfaces (raw-packet event, codec filters,
// limbo, virtual backend) arrive as minor bumps.
package infrarust:plugin@0.1.0;

world plugin {
    import types;
    import log;
    import event-bus;
    import player-registry;
    import server-manager;
    import ban-service;
    import config-service;
    import command-manager;
    import scheduler;

    export guest;
}
